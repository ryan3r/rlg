# Description

A dungeon generator rooms are represented by "."s and corridors are represented
by "#"s.

# Usage

```
build/dungeon [seed]
```

The first argument is the seed to use for generation.  If no seed is given it
will default to a random seed.

# Tests

There are tests for the room placing code which can be run with `make test`.
